{
  "name": "data-structures-typescript",
  "version": "1.0.0",
  "description": "Used to learn data structures in TypeScript",
  "private": true,
  "scripts": {
    "clean": "rimraf coverage",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:dev": "vitest dev",
    "test:cov": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "prettier": "^3.3.1",
    "rimraf": "^5.0.7",
    "tsx": "^4.11.2",
    "vitest": "^1.6.0"
  },
  "author": "Edwin Hernandez"
}
