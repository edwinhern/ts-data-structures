{
  "name": "ts-data-structures",
  "version": "1.0.0",
  "description": "A personal project to learn and implement various data structures in TypeScript by creating classes and writing test files to validate them.",
  "private": true,
  "scripts": {
    "clean": "rimraf coverage",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:dev": "vitest dev",
    "test:cov": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "prettier": "^3.3.1",
    "rimraf": "^5.0.7",
    "tsx": "^4.11.2",
    "vitest": "^1.6.0"
  },
  "author": "Edwin Hernandez"
}
